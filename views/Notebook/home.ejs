<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>PaperCast - Smart Document Intelligence</title>
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;500;600&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
    <link rel="stylesheet" href="styleAditya.css">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            font-family: 'Poppins', sans-serif;
        }

        body {
            background: #f8f9fa;
        }

        .container {
            max-width: 1200px;
            margin: 0 auto;
            padding: 2rem;
        }

        .hero {
            background: linear-gradient(135deg, #4b4453 0%, #3a3340 100%);
            color: white;
            padding: 4rem 2rem;
            border-radius: 1rem;
            margin-bottom: 3rem;
            position: relative;
            overflow: hidden;
        }

        .hero::after {
            content: '';
            position: absolute;
            top: -50%;
            left: -50%;
            width: 200%;
            height: 200%;
            background: linear-gradient(transparent 0%, rgba(255, 255, 255, 0.1) 100%);
            transform: rotate(45deg);
            animation: shine 8s infinite;
        }

        @keyframes shine {
            0% {
                transform: rotate(45deg) translateX(-200%);
            }

            100% {
                transform: rotate(45deg) translateX(200%);
            }
        }

        .hero-content {
            position: relative;
            z-index: 1;
            text-align: center;
            animation: fadeIn 1s ease-out;
        }

        h1 {
            font-size: 3.5rem;
            margin-bottom: 1.5rem;
            font-weight: 600;
        }

        .hero p {
            font-size: 1.2rem;
            margin-bottom: 2rem;
            opacity: 0.9;
        }

        .cta-button {
            background: #746880;
            color: white;
            padding: 1rem 2.5rem;
            border: none;
            border-radius: 2rem;
            font-size: 1.1rem;
            cursor: pointer;
            transition: all 0.3s ease;
            box-shadow: 0 4px 15px rgba(44, 42, 61, 0.8);
        }

        .cta-button:hover {
            transform: translateY(-3px);
            box-shadow: 0 6px 20px rgba(44, 42, 61, 0.9);
        }

        .features {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
            gap: 2rem;
            margin-top: 3rem;
        }

        .feature-card {
            background: white;
            padding: 2rem;
            border-radius: 1rem;
            text-align: center;
            transition: transform 0.3s ease;
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.05);
        }

        .feature-card:hover {
            transform: translateY(-10px);
            box-shadow: 0 8px 15px rgba(0, 0, 0, 0.1);
        }

        .feature-icon {
            font-size: 2.5rem;
            color: #3a3340;
            margin-bottom: 1rem;
        }

        .feature-card h3 {
            color: #2e2a33;
            margin-bottom: 1rem;
        }

        .feature-card p {
            color: #5a5261;
            line-height: 1.6;
        }

        @keyframes fadeIn {
            from {
                opacity: 0;
                transform: translateY(20px);
            }

            to {
                opacity: 1;
                transform: translateY(0);
            }
        }

        @media (max-width: 768px) {
            h1 {
                font-size: 2.5rem;
            }

            .hero {
                padding: 3rem 1.5rem;
            }
        }
    </style>
</head>

<body>
    <%- include("../includes/navbar.ejs") %>
    <div class="container">
        <section class="hero">
            <div class="hero-content">
                <h1>Welcome to PaperCast</h1>
                <p>Your Intelligent Document Companion<br>
                    Transform documents into actionable insights with AI-powered analysis</p>
                <form action="/dashboard" method="GET">
                    <button class="cta-button">Create Your First Project</button>
                </form>
            </div>
        </section>

        <div class="features">
            <div class="feature-card">
                <i class="fas fa-upload feature-icon"></i>
                <h3>Smart Upload</h3>
                <p>Upload documents and let PaperCast analyze content, extract key insights, and answer complex
                    questions</p>
            </div>

            <div class="feature-card">
                <i class="fas fa-brain feature-icon"></i>
                <h3>AI Analysis</h3>
                <p>Convert complex materials into summaries, FAQs, and executive briefings automatically</p>
            </div>

            <div class="feature-card">
                <i class="fas fa-users feature-icon"></i>
                <h3>Team Collaboration</h3>
                <p>Create shared knowledge bases and collaborate in real-time with your organization</p>
            </div>
        </div>
    </div>
<%- include("../includes/footer.ejs") %>
    <script>
        // Add scroll animation for feature cards
        const observer = new IntersectionObserver((entries) => {
            entries.forEach(entry => {
                if (entry.isIntersecting) {
                    entry.target.style.animation = `fadeIn 0.6s ease-out forwards`;
                }
            });
        });

        document.querySelectorAll('.feature-card').forEach((card) => {
            card.style.opacity = '0';
            observer.observe(card);
        });

        // Add hover effect to CTA button
        document.querySelector('.cta-button').addEventListener('mouseover', function () {
            this.style.transform = 'scale(1.05)';
        });

        document.querySelector('.cta-button').addEventListener('mouseout', function () {
            this.style.transform = 'scale(1)';
        });
    </script>
     <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz" crossorigin="anonymous"></script>
</body>

</html>